/* 
  Criar uma soma foi fácil né? Programação nem é tão difícil assim rsrs
  Para subtraírmos é tão fácil quanto! Para isso, iremos criar um outro
  trecho de código, que irá novamente pegar dois números, e imprimir
  no console: "x - y = z", da mesma forma que fizemos no exercício anterior.
*/

// Código base para utilizar:
const num1 = Math.floor(Math.random() * 100)
const num2 = Math.floor(Math.random() * 100)

// Crie sua solução abaixo:
