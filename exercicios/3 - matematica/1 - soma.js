/*
  Vamos usar essa seção de matemática pra criar algumas funções de uma
  calculadora, que tal?
  A primeira delas é a soma! Para isso eu criei duas variáveis com dois
  números aleatórios de 0 a 100. Quero que você imprima no console:
  "x + y = z", substituíndo x e y pelas variáveis, e z pelo resultado da soma.
*/

// Código base para utilizar:
const num1 = Math.floor(Math.random() * 100)
const num2 = Math.floor(Math.random() * 100)

// Crie sua solução abaixo:
