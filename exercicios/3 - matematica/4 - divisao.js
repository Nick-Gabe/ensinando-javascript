/* 
  È na divisão que as coisas começam a complicar. Pois se você quiser
  por exemplo um resultado inteiro, só a divisão em si não seguirá essa
  regra. Nela é permitido haver resultados decimais, como "1 / 2 = 0.5".

  Então não se espante se ver um número com muitas casas decimais, é que
  linguagens no geral não lidam tão bem com isso ainda.

  Mas quero que utilize as variáveis disponibilizadas e imprima no console:
  "x / y = z". Onde x e y são as variáveis, e z o resultado da divisão.
*/

// Código base para utilizar:
const num1 = Math.floor(Math.random() * 100)
const num2 = Math.floor(Math.random() * 100)

// Crie sua solução abaixo:
