/* 
  Multiplicação é algo bem comum também, e super simples de pegar.
  Nós iremos criar um trecho que código que recebe dois números.
  Devemos apenas imprimir "a x b = z" (pode trocar x por * ou . se preferir)
  onde "a" e "b" são as variáveis, e "z" o resultado da multiplicação.
*/

// Código base para utilizar:
const num1 = Math.floor(Math.random() * 100)
const num2 = Math.floor(Math.random() * 100)

// Crie sua solução abaixo:
