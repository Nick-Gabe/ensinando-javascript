/* 
  Neste cenário temos um número que representa a idade de uma pessoa (1-100).
  Queremos descobrir se para a pessoa de tal idade é ou não obrigatório votar.

  Caso a idade seja abaixo de 18 OU acima de 71, imprimimos "Não é obrigatório votar".
  Caso contrário, imprimimos "É obrigatório votar".
*/

// Código base para utilizar:
const idade = Math.floor(Math.random() * 99) + 1

// Crie sua solução abaixo:
